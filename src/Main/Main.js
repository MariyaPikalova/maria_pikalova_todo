import './main.scss';
import React from 'react';
import { Aside } from '../Aside';
import { Section } from '../Section';

export const Main = () => (

        <div className = "main-wrap">
            <Aside/>
            <Section/>
        </div>
);